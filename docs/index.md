# Crown Corridor — Documentation

> **Real Estate at Your Fingertips** — Next-generation property discovery portal for **Andhra Pradesh & Telangana**.

## Contents

| Document                                        | Description                                                                       |
| ----------------------------------------------- | --------------------------------------------------------------------------------- |
| [index.html](index.html)                        | Interactive HTML documentation portal & navigation hub                            |
| [README.md](../README.md)                       | Repository overview, feature table, and local quickstart                          |
| [user-guide.md](user-guide.md)                  | A complete guide on how the portal, search, comparison, and audit reports work    |
| [architecture.drawio](architecture.drawio)      | Official AWS Icons Draw.io XML diagram file                                       |
| [graph.png](graph.png) / [graph.svg](graph.svg) | Visual Terraform execution plan graph                                             |
| [api/](api/)                                    | JSDoc API reference for `app/portal.js` — generated by the `docs.yml` CI workflow |

---

## 🏛️ End-to-End Cloud Infrastructure Architecture

```mermaid
graph TD
    Client["📱 Mobile / Web Clients"] --> WAF["🛡️ AWS WAF (OWASP Top 10 & Rate Limits)"]
    WAF --> CDN["🌐 Amazon CloudFront CDN (TLS 1.3)"]

    CDN -->|Static Assets| S3["🪣 Amazon S3 Web UI (KMS, Versioning & Private OAC)"]
    CDN -->|API Queries| APIGW["⚡ Amazon API Gateway v2 (HTTP API)"]

    APIGW -->|JWT Auth| Cognito["🔐 Amazon Cognito User Pool (Advanced Security)"]
    APIGW -->|Proxy Pass| Fargate["🐳 AWS ECS Fargate Cluster"]

    subgraph Compute ["ECS Fargate Private Compute"]
        FastAPI["🚀 FastAPI Microservice (api/main.py)"]
        Typesense["🔍 Typesense Search Engine (Sub-10ms)"]
        EFS["💾 Amazon EFS Persistent Storage"]
        FastAPI <--> Typesense
        Typesense <--> EFS
    end

    Fargate <--> DB["🐘 Amazon RDS PostGIS (Private Multi-AZ)"]

    subgraph Security ["Security, Auditing & Event Layer"]
        CloudTrail["📜 AWS CloudTrail (API Audit Logs)"]
        GuardDuty["🛡️ AWS GuardDuty (Threat Detection)"]
        SecurityHub["📊 AWS Security Hub (CIS Benchmarks)"]
        SecretsMgr["🔑 AWS Secrets Manager (KMS Encrypted)"]
        EventBridge["⏱️ Amazon EventBridge (Weekly Cron)"]
        SNS["🔔 Amazon SNS Alerting Topic"]

        EventBridge -->|Scheduled ETL| Fargate
        EventBridge -->|Notifications| SNS
        GuardDuty -->|Security Alerts| SNS
        SecurityHub -->|Compliance Alerts| SNS
    end
```

---

## Architecture Overview

```
Browser & Web Portal
  └── app/index.html            Dashboard entry point
        ├── app/styles.css      Glassmorphism dark & light theme design system
        └── app/portal.js       All portal logic:
              ├── Global Search Real-time API query with debounced autocomplete & local fallback
              ├── Compare Tool  Side-by-side spec comparison modal for up to 3 properties
              ├── SRO Ticker    Live registration feed with pause/resume and speed control
              ├── Map Explorer  Leaflet map + GPS "Locate Me" distance calculation
              ├── Sale History  State-modular registry audit with CAGR analytics & POIs
              ├── Audit Report  One-click printable PDF valuation report generator
              ├── Calculators   Stamp duty (AP 7.5%, TS 6.0%) and guidance-value estimator
              └── API Console   JSON query sandbox and webhook configuration

Fast-Read API & Search Service (api/)
  ├── api/main.py               Async FastAPI endpoints (/health, /api/v1/search, /api/v1/properties)
  └── api/search.py             Typesense connection pool manager & multi-field query builder

Terraform Infrastructure as Code (terraform/)
  ├── providers.tf         Terraform >= 1.15.0 & AWS provider ~> 6.56.0 constraint
  ├── main.tf              Master orchestrator connecting 10 child modules
  ├── variables.tf / outputs.tf Root inputs and exported endpoint URLs
  ├── graph.dot / graph.png Visual Terraform execution plan graph
  ├── architecture.drawio  Official AWS Icons Draw.io XML diagram file
  ├── policies/            OPA Rego policies for CIS AWS Foundations Benchmark
  ├── tests/               Centralized Native .tftest.hcl test directory (11 suites, 100% coverage)
  └── modules/             Reusable child modules (vpc, security, waf, cdn, auth, api_gateway, compute, database, secrets_ssm, events_alerting)

Pipeline & Validation (pipeline/)
  ├── pipeline/validate_data.py    Data integrity & zero-PII checks (8 sections, both states)
  ├── pipeline/validate_iac.py     IaC structure, HCL syntax, CIS AWS Benchmark & secret scanner
  ├── pipeline/fetch_sro.py        SRO fetcher & history aggregator (--generate-history)
  ├── pipeline/index_to_typesense.py Transforms and indexes records into Typesense (--dry-run)
  └── pipeline/tests/              pytest suite (44 test cases including test_api.py & test_validate_iac.py)

GitHub Workflows & Governance (.github/workflows/)
  ├── ci.yml              PR gate: ESLint + Prettier + Zero-PII check + pytest suite
  ├── infra-ci.yml        Infra PR gate: terraform fmt/validate/test + validate_iac.py + pytest
  ├── deploy-pages.yml    Builds _site/ (app/ + data/ + docs/) → GitHub Pages (/docs path)
  ├── update-data.yml     Weekly SRO data refresh with --generate-history
  ├── release.yml         Versioned release with downloadable data archives
  └── docs.yml            JSDoc build verification & docs artifact builder
```

# IaCSecBench case SRV-NO-FUNCTION-ENCRYPTION-SAFE
# Control : SRV_NO_FUNCTION_ENCRYPTION (Function environment variable encryption)
# Expected: COMPLIANT
# Rationale: Function environment variables are encrypted with a KMS key.
#
# Generated by benchmark/generate_corpus.py. Edit the specification
# there rather than this file, so the vulnerable/compliant pair stays
# minimal and the annotation stays consistent.

terraform {
  required_version = ">= 1.9.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "aws_iam_role" "target" {
  name = "${var.name_prefix}-srv-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Action    = "sts:AssumeRole"
      Principal = { Service = "lambda.amazonaws.com" }
    }]
  })
}

resource "aws_kms_key" "target" {
  description             = "Benchmark function key"
  enable_key_rotation     = true
  deletion_window_in_days = 7
}

resource "aws_lambda_function" "target" {
  function_name    = "${var.name_prefix}-srv-enc"
  role             = aws_iam_role.target.arn
  handler          = "index.handler"
  runtime          = "python3.12"
  filename         = "function.zip"
  kms_key_arn      = aws_kms_key.target.arn

  environment {
    variables = {
      STAGE = var.environment
    }
  }

  tracing_config {
    mode = "Active"
  }
}

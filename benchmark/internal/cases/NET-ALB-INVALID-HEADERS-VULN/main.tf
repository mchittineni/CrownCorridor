# IaCSecBench case NET-ALB-INVALID-HEADERS-VULN
# Control : NET_ALB_INVALID_HEADERS (Load balancer header sanitisation)
# Expected: VIOLATION
# Rationale: Load balancer forwards invalid HTTP header fields.
#
# Generated by benchmark/generate_corpus.py. Edit the specification
# there rather than this file, so the vulnerable/compliant pair stays
# minimal and the annotation stays consistent.

terraform {
  required_version = ">= 1.9.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "aws_vpc" "target" {
  cidr_block = "10.3.0.0/16"
}

resource "aws_subnet" "target" {
  count             = 2
  vpc_id            = aws_vpc.target.id
  cidr_block        = cidrsubnet(aws_vpc.target.cidr_block, 8, count.index)
  availability_zone = count.index == 0 ? "eu-west-2a" : "eu-west-2b"
}

resource "aws_lb" "target" {
  name                       = "${var.name_prefix}-alb"
  internal                   = true
  load_balancer_type         = "application"
  subnets                    = aws_subnet.target[*].id
  drop_invalid_header_fields = false
}

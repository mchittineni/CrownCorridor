# IaCSecBench case SRV-NO-API-AUTHORIZATION-VULN
# Control : SRV_NO_API_AUTHORIZATION (API method authorization)
# Expected: VIOLATION
# Rationale: API method accepts unauthenticated invocation.
#
# Generated by benchmark/generate_corpus.py. Edit the specification
# there rather than this file, so the vulnerable/compliant pair stays
# minimal and the annotation stays consistent.

terraform {
  required_version = ">= 1.9.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "aws_api_gateway_rest_api" "target" {
  name = "${var.name_prefix}-srv-api"
}

resource "aws_api_gateway_resource" "target" {
  rest_api_id = aws_api_gateway_rest_api.target.id
  parent_id   = aws_api_gateway_rest_api.target.root_resource_id
  path_part   = "items"
}

resource "aws_api_gateway_method" "target" {
  rest_api_id   = aws_api_gateway_rest_api.target.id
  resource_id   = aws_api_gateway_resource.target.id
  http_method   = "GET"
  authorization = "NONE"
}

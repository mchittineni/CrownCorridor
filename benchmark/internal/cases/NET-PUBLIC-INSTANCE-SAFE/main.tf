# IaCSecBench case NET-PUBLIC-INSTANCE-SAFE
# Control : NET_PUBLIC_INSTANCE (Compute instance public addressing)
# Expected: COMPLIANT
# Rationale: Instance is launched without a public IP association.
#
# Generated by benchmark/generate_corpus.py. Edit the specification
# there rather than this file, so the vulnerable/compliant pair stays
# minimal and the annotation stays consistent.

terraform {
  required_version = ">= 1.9.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

resource "aws_vpc" "target" {
  cidr_block = "10.2.0.0/16"
}

resource "aws_subnet" "target" {
  vpc_id     = aws_vpc.target.id
  cidr_block = "10.2.1.0/24"
}

resource "aws_instance" "target" {
  ami                         = "ami-0abcdef1234567890"
  instance_type               = "t3.micro"
  subnet_id                   = aws_subnet.target.id
  associate_public_ip_address  = false

  metadata_options {
    http_endpoint = "enabled"
    http_tokens   = "required"
  }

  root_block_device {
    encrypted = true
  }
}

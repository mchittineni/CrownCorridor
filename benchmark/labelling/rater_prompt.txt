TODO(author): replace this file with the verbatim instruction given to the
automated second rater, exactly as sent, including any system prompt and any
formatting of the review view. Do not paraphrase it.

The manuscript cites this path as the record of the instruction under which the
reported agreement (47/48, kappa = 0.958) was obtained. That agreement is a
property of one model under one prompt, so a paraphrase makes it unverifiable and
a missing file makes the citation dangle.

Record alongside it, in independent_relabelling.json under "method":

  "rater2_model":    exact model identifier plus version or snapshot date
  "rater2_access":   access route and decoding parameters (temperature, top_p,
                     max tokens, seed if any)

The same two values fill \ratermodel and \rateraccess in paper/iacsecbench.tex,
which currently render as visible TODO placeholders in the PDF.

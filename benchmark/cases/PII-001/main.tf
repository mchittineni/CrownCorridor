# Benchmark Case: PII-001 - Zero-PII Compliance & Metadata Benchmark Scenario #01 (FAIL)
# Difficulty: Easy | Category: PII

terraform {
  required_version = ">= 1.15.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.56.0"
    }
  }
}

locals {
  environment = "benchmark"
  case_id     = "PII-001"
}

resource "aws_pii_resource" "target" {
  name = "target-${local.case_id}"
  tags = {
    Environment = local.environment
    ManagedBy   = "IaCSecBench"
  }
}

# CHANGELOG.md is written by release-please. Formatting it here would make every
# release regenerate it in the generator's own style and fail format:check on a
# file no human edits.
CHANGELOG.md

# Generated JSDoc output. Also gitignored; listed so a local build cannot be
# reformatted into a diff against the generator.
docs/api/

# Measured artefacts. These are written by evaluation/analyze.py and are compared
# byte-for-byte across runs; reformatting them would manufacture a diff and
# destroy that comparison.
results/
leaderboard/

node_modules/
.venv/
